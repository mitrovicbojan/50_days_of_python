'''
Task Manager

Create a Task class and a TaskManager class.

Task

Attributes: title, description, completed (default False).

Methods: mark_done(), __str__() → shows title and “Completed” or “Not completed”.

TaskManager

Attribute: tasks → list of Task objects.

Methods:

add_task(task) → adds task to list.

list_tasks() → prints all tasks with status.

complete_task(title) → finds task by title and marks it done. 
'''